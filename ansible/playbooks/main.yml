---
- name: FreeBSD 14 provisioning
  hosts: all
  gather_facts: true

  vars_files:
    - ../vars/upgrade.yml
    - ../vars/common_pkgs.yml
    - ../vars/user_setup.yml
    - ../vars/virtualbox_guest.yml

  roles:
    - pluggero.upgrade
    - pluggero.common_pkgs
    - pluggero.user_setup
    - pluggero.virtualbox_guest
